XSS

Official Definition: XSS flaws occur whenever an application takes untrusted data and sends it to a web browser without proper validation or escaping. XSS allows attackers to execute scripts in the victim’s browser which can hijack user sessions, deface web sites, or redirect the user to malicious sites.

Simple Definition: ?

Hacker definition: Find a way to post your own content to a page (EG comments) or a URL that reflects part of itself into the page (EG search) that isn't sanitized. Add your own JS payload. Profit.

Incident: ?

Demo: JS code injection - BEEF http://beefproject.com/ - Maybe pop a shell?

Fix: ?